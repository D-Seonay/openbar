on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Redéployer la stack
        run: |
          cd /opt/bardenoa
          git pull
          sudo docker compose up -d --build
